# Secrets (do not commit real credentials)

Place Google OAuth client JSON here as:

- `credentials.json` — OAuth Desktop client from Google Cloud Console
- `token.json` — auto-created after first successful browser consent (do not commit)

This folder is gitignored except this README and `.gitkeep`.
Never commit real Client ID / Client Secret / token files.
